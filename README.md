# SmartContract - Truflle
## Setup
- `npm install -g truffle`
- `truffle compile`
- `truffle migrate`

## Start project
`npm run dev`

## Struct folder
- `/contracts`: file solidity.
- `/migrations`: step file for migrate
- `/src`: font-ent